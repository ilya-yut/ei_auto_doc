# Parameter Configuration Guidelines - prompt for any EI / function module

Generate only the `### Parameter Configuration Guidelines` subsection for an SAP Exception Indicator (EI).

---

## Role and inputs

You are documenting parameter configuration for an SAP EI function.

Inputs:
1. Output structure / fields file
2. Parameters file (Available fields -> Parameters sheet)
3. ABAP code file
4. Selected parameters file: C:\vibe code dev\ei_auto_doc\input\_Parameters.docx


**Output scope:** only section 04 (`### Parameter Configuration Guidelines`), nothing else.

---

## Hard rules (highest priority)

0. **SAP-canonical explanations: `input/params_dictionary.xlsx` + optional `input/checked params.txt`**  
   - **`input/params_dictionary.xlsx`** (sheet **`dictionary`**, columns **`parameter`** and **`suggested/corrected explanation`**) holds curated explanations keyed by **parameter Field name** (same spelling as the Parameters sheet / Reference Table, e.g. `BACKDAYS`, `SHKZG`).  
   - **`input/checked params.txt`** (optional): one parameter name per line (same spelling as Field). Lines starting with `#` after optional whitespace are comments. **If this file is missing or contains no non-blank lines, ignore the yellow-highlight rule below** (dictionary text still applies when present).  
   - **For each parameter, apply in order:**  
     1. **In `params_dictionary.xlsx` AND listed in `checked params.txt`:** use the dictionary explanation as the **main explanation paragraph** **verbatim** (no paraphrase). **Do not** wrap it in `<mark>` (`python scripts/pipeline/pipeline.py verify` fails if you do). **Do not** append or prepend EI-specific clauses (no extra table/field names, no “on **KNVV**”, no “filters via …” suffixes)—put EI-specific behavior in **Parameter Relationships** or code-derived **Options** only.
     2. **In `params_dictionary.xlsx` but NOT in `checked params.txt`** (and `checked params.txt` is active as above): use the dictionary explanation **verbatim** (same “no extra sentences” rule as bullet **1**), wrapped as **`<mark>…</mark>`** around the **entire** dictionary paragraph only (so Word export can render yellow highlight; see pipeline `md_to_docx`). Escape any `<` / `>` inside the explanation as HTML entities if needed.
     3. **Not in `params_dictionary.xlsx`:** write a concise, SAP-style explanation yourself (data element / domain meaning, typical usage in monitoring selection), still following all other rules below (same as previous “no dictionary row” behavior).
   - **Exceptions (dictionary does not override mandatory boilerplate):**  
     - **`BACKDAYS`:** keep the **mandatory verbatim window sentence** (and anchor line rules) from **§3a** exactly as specified; you may place the dictionary paragraph **immediately before** that mandatory sentence if the dictionary adds canonical context, or rely on the dictionary only if it already contains the same mandatory sentence. Apply the `<mark>` rule only to the dictionary paragraph lines, not to the mandatory BACKDAYS sentence.  
     - **`USER_FLD` / `USR_FLD`:** keep the **mandatory DRL narrative** from **§3b** verbatim first; if the dictionary contains an extra explanation for the same parameter, append it **after** the DRL block (and after any code-derived literal list), not instead of it; apply `<mark>` to that appended dictionary paragraph when rule **0** bullet **2** applies.  
     - **Options-only subsections** (`**… Options:**`): still list values as required elsewhere; dictionary text applies to the **main** paragraph unless the dictionary row clearly documents a specific option line (then align option bullets with it). Do **not** wrap individual option bullets in `<mark>` for this rule unless the whole main paragraph is dictionary-only text.

1. **Order and coverage**
   - Follow the **Parameters Reference Table** order: fields **alphabetically A–Z by Field name** (case-insensitive), same as a sorted Parameters sheet.
   - Cover every parameter.
   - Group only true serial ranges (for example `KEY1 - KEY10`, `MSGV1 - MSGV4`) when business meaning is shared.

1b. **Unused parameters (`**Not in use**`)**
   - When the pipeline marks a parameter as unused (declared in the interface but not on the output structure sheet and not used in ABAP to affect results), its block must include a separate line **`Not in use`** immediately **after** the main description paragraph(s) and **before** any `**… Options:**` subsection.
   - Do **not** remove or paraphrase **`Not in use`** when `prepare --generate-037` inserted it.
   - Unused parameters must **not** appear in **Parameter Relationships** (section 05) or **Default Values / Practical Examples** (section 06).
   - The IMPORTANT intro line explains that parameters marked **`Not in use`** do not change results for this EI.

2. **No generic/template text** (does not apply where you are pasting **dictionary** text verbatim per rule **0**)
   - Do NOT use fill-in templates such as:
     - "`PARAM` is used with its business meaning `DESC` ..."
     - "Parameter used for monitoring/processing ..."
   - Main explanation must be parameter-specific and concrete.

3. **Parameter-specific distinctness**
   - Do not reuse identical main-description sentences across different parameters.
   - Confusable parameters must be explicitly differentiated (for example `MSGNO` vs `MSGTP` vs `MSGTXT`).
   - For related date/time fields (for example start/end/schedule/release/change timestamps), do not repeat one generic sentence template; explain each field's distinct role.

3a. **BACKDAYS (mandatory window sentence when BACKDAYS appears in the Parameters sheet)**

When **BACKDAYS** is listed in the Parameters file, the **BACKDAYS** block must always include the monitoring-window sentence below **verbatim** (copy **character-for-character**; you may use either Unicode en-dash `–` (U+2013) or hyphen-minus `-` for the two dashes in that sentence only):

**First sentence (exact):**

`BACKDAYS defines the historical monitoring window by specifying how many days backward from today to retrieve records. 0 – today, 1 – today + yesterday etc.`

**Second line (anchor — only when it can be stated from evidence):**

- If **DATE_REF_FLD** exists in the Parameters sheet for this function, add **exactly**:

`Backdays is based on DATE_REF_FLD field.`

- If **DATE_REF_FLD** does **not** exist: read the ABAP. Add a **second line** **only** when the source clearly shows which SAP **date field** the BACKDAYS lookback applies to (e.g. `TABLE~FIELD IN R_DATUM`, `FIELD IN R_CREDATE`, `TABLE~FIELD IN R_ERDAT`, or an equivalent explicit selection). Use this exact pattern (replace only `FIELD` with that technical name, same casing as SAP):

`Backdays is based on FIELD`

- If you **cannot** determine that field from the available ABAP, **do not** add any `Backdays is based on …` line—**do not** guess or default to a field name (e.g. do not assume ERDAT).

Do **not** paraphrase, merge, bold, or append extra clauses to these mandatory lines when they are present. Do **not** use internal ABAP range variable names elsewhere in the BACKDAYS block (no `R_DATUM`, `SY-DATUM`, `DATE_FROM`, etc.).

3c. **FORWDAYS (mandatory forward window sentence when FORWDAYS appears in the Parameters sheet)**

When **FORWDAYS** is listed in the Parameters file, the **FORWDAYS** block must always include the forward monitoring-window sentence below **verbatim** (copy **character-for-character**; you may use either Unicode en-dash `–` (U+2013) or hyphen-minus `-` for the two dashes in that sentence only):

**First sentence (exact):**

`FORWDAYS defines the historical monitoring window by specifying how many days forward from today to retrieve records. 0 – today, 1 – today + tomorrow etc.`

**Second line (anchor — only when it can be stated from evidence):**

- If **DATE_REF_FLD** exists in the Parameters sheet for this function, add **exactly**:

`Forwdays is based on DATE_REF_FLD field.`

- If **DATE_REF_FLD** does **not** exist: read the ABAP. Add a **second line** **only** when the source clearly shows which SAP **date field** the forward (or FORWDAYS-adjusted) lookback applies to (same evidence as for BACKDAYS: e.g. `TABLE~FIELD IN R_ERDAT`, `FIELD IN R_BEDAT`, or when `LV_BACKDAYS = LV_FORWDAYS` precedes the same range copy as BACKDAYS). Use this exact pattern (replace only `FIELD` with that technical name, same casing as SAP):

`Forwdays is based on FIELD`

- If you **cannot** determine that field from the available ABAP, **do not** add any `Forwdays is based on …` line—**do not** guess.

Do **not** paraphrase, merge, bold, or append extra clauses to these mandatory lines when they are present. Do **not** use internal ABAP range variable names elsewhere in the FORWDAYS block (no `R_DATUM`, `SY-DATUM`, `DATE_FROM`, etc.).

3b. **`USER_FLD` / `USR_FLD` (mandatory DRL narrative; optional ABAP literals only)**

When **`USER_FLD`** or **`USR_FLD`** appears in the Parameters file, that parameter’s block in section 04 must follow **all** of the following:

1. **Mandatory fixed narrative (verbatim):** Immediately after the `**USER_FLD** (...)` or `**USR_FLD** (...)` heading line, insert the following text **character-for-character** (line breaks as shown; do not merge into one paragraph; do not add bold inside this block except the existing capitalization in “How DRL Works:” as plain text):

The USER_FLD parameter serves a dual purpose in work process monitoring: filtering results and enabling dynamic notification routing through the Dynamic Recipient List (DRL) mechanism. Instead of notifying pre-assigned users, the DRL mechanism provides a flexible, role-based notification system that automatically determines the appropriate recipients based on the monitoring results.
How DRL Works:
When USER_FLD is specified, the system extracts values from that field in the monitoring result set
These extracted values are then used as recipient addresses for alert notifications
This creates a dynamic, role-based notification mechanism that adapts based on the actual data being monitored
The mechanism is much more flexible than defining and constantly updating lists of specific pre-assigned users

2. **Code-derived literals (only when present in ABAP):** After the mandatory block above, **only if** the supplied ABAP clearly shows explicit literal field-name values for `USER_FLD` (for example `USER_FLD = 'ERNAM'`, or `'VKORG' TO USER_FLD`, or the same on a helper variable that is clearly the USER_FLD carrier), append a short labeled list (recommended lead-in: `Explicit values from the supplied ABAP:`) with one bullet per literal (use the exact technical token as in code). **If the ABAP does not contain any such explicit literals, do not add anything** after the mandatory block—no invented examples, no “no fixed list” sentence, and **no** `**USER_FLD Options:**` / `**USR_FLD Options:**` subsection.

3. **Forbidden:** Do **not** use `**USER_FLD Sample values:**` / `**USR_FLD Sample values:**`. Do **not** use the deprecated sentence: `No fixed USER_FLD value list is defined in the available code for this EI.` Do **not** add a generic `**USER_FLD Options:**` block for this parameter.

3c. **`DATE_REF_FLD` (code-derived options first; generic fallback only when none found)**

When **`DATE_REF_FLD`** appears in the Parameters file, the `**DATE_REF_FLD Options:**` subsection must follow this policy:

1. **Code-first extraction:** Check ABAP for explicit date-reference literals, especially `CASE LV_DATE_REF_FLD` / `CASE DATE_REF_FLD` with `WHEN '...'` branches, and direct literal assignments to `DATE_REF_FLD`.
2. **If literals exist:** Output one line per literal in this style (no bold on the value token): `VALUE — short explanation`. In markdown you may prefix with `- ` for readability; the pipeline exports options to Word as **plain lines** (no list bullets).
   - Example style: `PWDCHGDATE — Date of Last Password Change (monitors password update patterns)`
   - `VALUE` must match the ABAP literal token exactly.
   - Use the same line pattern for other fixed-option blocks (such as `DURATION_UNIT`).
3. **If no literals are explicit in available ABAP:** Use generic guidance bullets (field name from EI source structure, environment-specific choice based on ABAP date axis).
4. **Forbidden when literals exist:** Do not output only placeholder/generic bullets while omitting code literals that are clearly present.

3d. **`AGGR_LEVEL` / `AGGLEVEL` (code-derived options with explicit values)**

When **`AGGR_LEVEL`** (or **`AGGLEVEL`**) appears in the Parameters file:

1. Parse ABAP first for explicit literals (for example `LV_AGGR_LEVEL = 'T'`, comparisons like `LV_AGGR_LEVEL <> 'S'`, or `CASE ... WHEN '...'`).
2. Emit `**AGGR_LEVEL Options:**` (or `**AGGLEVEL Options:**`) with one line per literal in this form: `VALUE — short explanation` (optional leading `- ` in markdown).
3. If ABAP has no explicit literals, use the standard fallback options:
   - `- T — Total aggregation across all servers.`
   - `- S — Server-level aggregation.`
4. Do not omit explicit literals that exist in ABAP.

4. **Code-first options policy**
   - Derive option values from code first (including called-function code if provided).
   - If code has no explicit values:
     - `DATE_REF_FLD`: follow **3c** (list ABAP literals when present; otherwise generic fallback).
     - `AGGR_LEVEL` / `AGGLEVEL`: follow **3d** (list ABAP literals when present; else `T` and `S` fallback).
     - `MANAGE_IN_UTC`, `STATE_COLOR`, `STATUS`, `QSTATE`, `STAT_*`: use standard SAP fallback values and label as fallback.
     - `USER_FLD` / `USR_FLD`: follow **3b** above (mandatory DRL text; list literals only when explicitly present in ABAP).

5. **Formatting**
   - Use heading once: `### Parameter Configuration Guidelines`
   - Each parameter block:
     - `**PARAM** (Description):`
     - Main explanation paragraph(s)
     - Optional `**PARAM Options:**` with one line per value (`VALUE — explanation`; optional `- ` prefix in markdown)
     - Optional connection subsection
   - Exception: when `DATE_REF_FLD` has code-derived literals, list them as lines in `VALUE — short explanation` form (optional `- ` prefix in markdown).

---

## Required options checks

Always include `**[PARAM] Options:**` when applicable:

- **Debit/credit indicator parameters:** When the Parameters sheet lists a parameter that is a debit/credit (Dr/Cr) indicator—by name (e.g. `SHKZG`, `BEKNZ`, `KZZUAB`) or when the **Description** column clearly indicates debit/credit or “Dr/Cr”—you **must** add `**[PARAM] Options:**` with the possible values. Derive value keys from the ABAP first (literals, comments, checks). If the code does not list literals, document the usual SAP single-character posting direction codes (**S** = Debit / Soll, **H** = Credit / Haben) and brief labels, unless application-specific keys are evident from the code or domain for this object—then document those keys instead.
- Boolean/flag parameters (CHAR(1), XFELD-like) with all code-used values.
- Fixed option params: `DATE_REF_FLD`, `TIME_REF_FLD`, `DURATION_UNIT`, `TIME_DIFF_UNIT`.
- Aggregation-level params: `AGGR_LEVEL`, `AGGLEVEL` (derive literals from ABAP first; fallback `T` and `S` only if no literals found).
- Severity/state/level-style params (for example `STATE_COLOR`, `STATE_ICON`, `STATUS`, `WP_TYPE`, `SLGMODE`, `RQARCHSTAT`).
- Mandatory global params when present:
  - `MANAGE_IN_UTC`
  - `STATE_COLOR`
  - `STATUS`
  - `QSTATE`
  - `STAT_WAIT`, `STAT_TRANSIT`, `STAT_OK`, `STAT_ERROR`, `STAT_INCONS`, `STAT_FUTURE`, `STAT_RETRY`, `STAT_DIRECT`, `STAT_ACTIVE`
- `USER_FLD` / `USR_FLD`: follow **3b** (not the generic `**… Options:**` rule below).

**DURATION_UNIT exact options text (mandatory verbatim):** Auto-generation and verify use `scripts/pipeline/pipeline.py` (`_DURATION_UNIT_OPTION_LINES`). Copy these four lines exactly under `**DURATION_UNIT Options:**` — do not paraphrase, bold the codes, or use em-dash bullets.
- H: Hours
- M: Minutes
- D: Days
- F: Full days for specific day filtering

**MANAGE_IN_UTC must include both:**
- `X` (UTC mode)
- blank/empty/local-time mode

---

## Quality requirements for main explanations

Each parameter main explanation must include at least one:
- source/derivation context,
- role in logic (selection/enrichment/classification/threshold/default),
- business effect.

Do not use implementation-noise prose (line numbers, low-level SQL narration) unless strictly necessary.

---

## Output format (strict)

Output exactly:

### Parameter Configuration Guidelines

**IMPORTANT:** This section provides configuration guidance for ALL [N] parameters listed in the Parameters Reference Table above.

Then parameter blocks in **the same A–Z order as the Parameters Reference Table** (by Field name).

Use these heading patterns only:
- `**PARAM** (Description):`
- `**PARAM Options:**`
- `**PARAM_A and PARAM_B Connection:**`
- grouped range forms like `**MSGV1 - MSGV4**`

One option per bullet line. No multi-option single lines.

---

## Inputs

**1. Output structure / fields file** (path or paste):
C:\vibe code dev\ei_auto_doc\input\Structure_SKN_S_SW_10_03_OPEN_PO_DET.xlsx

**2. Parameters file (Parameters sheet)** (path or paste):
C:\vibe code dev\ei_auto_doc\input\Available fields_SKN_S_SW_10_03_OPEN_PO_DET.xlsx

**3. ABAP code file** (path or paste):
C:\vibe code dev\ei_auto_doc\input\Code_SKN_S_SW_10_03_OPEN_PO_DET.txt

**4. Selected parameters file**:
Selected parameters file: [Path or "not provided"]

**5. Additional code (called function)** (optional):
[Additional code (called function) path or paste - replaced by prepare]

---

Respond with only the section content for `### Parameter Configuration Guidelines`.

# Copy-paste prompt: Parameters Reference Table section for any EI / function module

Use this in ChatGPT, Claude (web or API), or any LLM. Provide the Parameters (Name (Description)) file — or paste the parameters table — under the heading at the end, then send the whole thing.

---

```
You are documenting the Parameters Reference Table section for an SAP Exception Indicator (EI) / function module. You will receive ONE input: the Parameters (Name (Description)) file — typically the "Parameters" sheet from the Available fields Excel or an equivalent file containing the parameters table.

Your task: Output ONLY the "Parameters Reference Table" subsection for the EI document. Use the fixed intro wording from the benchmark and build the parameters table from the input file. **Group parallel table-side pairs** (same stem, suffix **1** and **2** only) that appear on **consecutive** rows in the source into **one row**: put **`NAME1 / NAME2`** in the Field column and combine descriptions with **` / `**. Example: `BACKDAYS1 / BACKDAYS2` with description `Table1- … / Table2- …`. For all **other** parameters (including interleaved sequences such as ATTR1, ATTR1_V, ATTR2, …), keep **one row per parameter**. **Sort all table rows alphabetically by Field name (A–Z, case-insensitive)** after any **1/2** merging (use the merged Field cell text as the sort key, e.g. `NAME1 / NAME2`). You may also use **`STEMi-j`** in the Field column only when indices *i*…*j* are **consecutive in the source with no other parameter between** (rare); the pipeline expands ranges when present. Do not output a document title, other sections, or preamble.

OUTPUT FORMAT (follow exactly):

### Parameters Reference Table

[Fixed intro — use this wording exactly:]
This table lists all configurable input parameters. Users set values for these parameters to filter and control the EI's data selection and processing logic.

[Table:]
Add a first column with the parameter number (1, 2, 3, …). Build the parameters table from the input file: use the same column headers and column order as in the input file, with the number column as the first column. If the input has columns Parameter, Description, Type, Length, Decimal, Data Element, Domain, use a markdown table with headers: # (or No.), Field (or Parameter), Description, Type, Length, Decimal, Data Element, Domain. **Row grouping:** merge **only** consecutive **1/2** parallel pairs into one row (`NAME1 / NAME2`). **Do not** merge longer serial runs that are interleaved with other parameters (e.g. do not merge ATTR1–ATTR5 into one row when ATTR1_V appears between ATTR1 and ATTR2). Include **every** parameter from the input; merged rows still represent the same total parameter count for the document (intro may say “all configurable parameters”; the Configuration Guidelines section states the exact count). Do not omit any parameters. **Renumber** the first column 1…N after sorting rows **A–Z by Field**.

Rules:
- Use the fixed intro sentence exactly as stated above. Do not reword or extend it.
- **First column (#):** Use a single number (1, 2, 3, …) for every **table row**. Merge consecutive **1/2** pairs into one row with **`NAME1 / NAME2`** in the Field column. Otherwise one row per parameter. Same columns as the input. **Sort rows A–Z by Field** (case-insensitive), then assign **#** in that order. Do not omit any parameters.
- Do not add or infer parameter rows not in the input; only copy from the input. The only structural change is adding the number column (1, 2, 3, …).
- Output ONLY the subsection: heading "### Parameters Reference Table", the fixed intro paragraph, and the markdown table. No document title, no other sections (e.g. no Parameter Configuration Guidelines, no preamble). Do **not** add a duplicate `## Parameters Reference Table` line under the `###` heading.
- Tone: professional. The intro is fixed; the table is built from input with **1/2 pair grouping** where applicable; all other parameters stay one row each.

Benchmark format reference (intro + table structure). Source: reference files\Explanation_Credit Memo Monthly volume by Payer_$1M in LC_200019_000012__EI__SW_10_01_ORD_VAL_TOT.docx

### Parameters Reference Table

This table lists all configurable input parameters. Users set values for these parameters to filter and control the EI's data selection and processing logic.

| # | Parameter | Description | Type | Length | Decimal | Data Element | Domain |
|---|-----------|-------------|------|--------|---------|--------------|--------|
| 1 | AEDAT | Changed On | DATS | 8 | 0 | AEDAT | DATUM |
| 2 | AGGR_FIELDS | Aggregation Fields |  | 0 | 0 |  |  |
| 3 | KEY1 | Field Name |  |  |  |  |  |
| 4 | KEY2 | Field Name |  |  |  |  |  |
| … | … | (include every parameter; one row per parameter, # = 1, 2, 3, …) |  |  |  |  |  |

Parameters (Name (Description)) file location or paste:
C:\vibe code dev\ei_auto_doc\input\Available fields_SKN_S_SW_01_01_SOURCE_SCAN.xlsx

---

Respond with ONLY the "### Parameters Reference Table" subsection (heading + fixed intro + full parameters table). No other text.
```

---

## How to use

1. Open this file and copy everything **inside** the triple backticks (from "You are documenting…" through "...No other text.").
2. Under **Parameters (Name (Description)) file location or paste:** provide either the file path to the Parameters file (e.g. the "Parameters" sheet from Available fields_*.xlsx) or paste the full parameters table (all columns, all rows — may be 120–130+ rows).
3. Paste the whole block into ChatGPT, Claude (web), or any LLM and send.
4. Use the model's reply as the Parameters Reference Table subsection in your EI document (under ## Parameters).

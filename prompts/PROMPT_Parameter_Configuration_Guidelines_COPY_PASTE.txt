You are documenting the Parameter Configuration Guidelines for an SAP Exception Indicator (EI) / function module. You will receive FOUR inputs: (1) Output structure / fields file, (2) Parameters (Name (Description)) file — the Parameters sheet from the Available fields Excel for this EI, (3) ABAP code file, (4) Selected parameters file — reusable explanation text for parameters (path: C:\vibe code dev\ei_auto_doc\reference\_Parameters.docx).

Your task: For EVERY parameter in the Parameters (Name (Description)) file, IN THE EXACT ORDER of that file, produce a configuration guideline entry. Output ONLY the "Parameter Configuration Guidelines" subsection. Do not output the Parameters Reference Table, Parameter Relationships, Default Values, Practical Configuration Examples, or any other section.

For each parameter:
1. Search the Selected parameters file (_Parameters.docx) for that parameter by name. If an explanation exists, copy it and adjust only: (a) table and field names (e.g. VBAK vs EKKO), (b) domain terms (e.g. sales order vs purchase order), (c) examples that refer to other EIs or parameters. Keep the structure and the remainder of the explanation unchanged. If none exists, write the explanation from the ABAP code and business logic; use SAP expertise only when crucially needed.
2. Use the Output structure / fields file: tie parameter semantics to the function's output (which fields are filtered, aggregated, or populated). If unclear from code, use overall SAP expertise.
3. Parameter references: In the main explanation and in Connection subsections, reference ONLY parameters that appear in the Parameters (Name (Description)) file for this EI. Do not mention parameters that are not in that file (e.g. do not mention BACKDAYS or DATE_REF_FLD if they are not in the Parameters sheet). Describe behavior using only parameters that exist in the Parameters file.
4. Structure: Every parameter has a header and at least ONE sub-section (main explanation). Some have 2–3 sub-sections:
   - Always: Main explanation (what it does, how it is used, relation to output).
   - Optional: "Possible values / Options" — ONLY for parameters with specific non-generic values (e.g. DATE_REF_FLD, STATUS, AGGR_PERIOD, DURATION_UNIT, DC_IND). Not for generic (e.g. BUKRS). Default: short bullets (value key + brief label, e.g. "- **D**: Debtor (customer)"). Exception: if the parameter is in the Selected parameters file, follow that file's structure and level of detail for Options; if not in that file, keep Options short unless crucial for clarity.
   - Optional: "Work together / Connection" — ONLY when this parameter is designed to work with others. Reference ONLY parameters from the Parameters file (e.g. BACKMONTHS + COMPMONTHS, DURATION + DURATION_UNIT). Decide from code, Selected parameters file, SAP expertise.

OUTPUT FORMAT (follow exactly):

### Parameter Configuration Guidelines

**IMPORTANT:** This section provides configuration guidance for ALL [N] parameters listed in the Parameters Reference Table above.

**[PARAM1]** ([Description from Parameters file]):

[Main explanation – at least one paragraph. Optional: **Possible values / Options** and/or **Work together / Connection** sub-sections if applicable.]

**[PARAM2]** ([Description from Parameters file]):

[Same: 1–3 sub-sections.]

[Continue for every parameter in the EXACT ORDER of the Parameters file. Do not output Parameters Reference Table, Parameter Relationships, or anything else.]

Rules:
- Order: Follow the EXACT order of parameters from the Parameters (Name (Description)) file (same as Parameters Reference Table). Include every parameter; do not skip.
- Count: Replace [N] with the actual number of parameters.
- Header: **PARAM_NAME** (Description from table): — use description exactly as in the Parameters file.
- Sub-sections: At least main explanation per parameter. Add "Possible values" only for specific non-generic values; add "Work together" only when parameters clearly work together. Use these exact heading patterns: for possible values/options, **[PARAM_NAME] Options:** (e.g. **AGGR_PERIOD Options:**, **DATE_REF_FLD Options:**); for relationship between two parameters, **[PARAM_A] and [PARAM_B] Connection:** (e.g. **BACKMONTHS and COMPMONTHS Connection:**); for bullet list of how parameters interact, **How They Work Together:**; for combined examples, **Combined Configuration Examples:** or **Configuration Impact:**. Do not invent new heading phrasings.
- Options brevity: By default list only value keys and short explanations (e.g. "- **D**: Debtor (customer)"). If the parameter is in the Selected parameters file, use that file's structure and level of detail for Options; if not, keep Options short unless crucial for clarity.
- Source priority: Selected parameters file first (if found); then code and business logic; SAP expertise when needed.
- Tone: Professional. No implementation details (no internal function names, line numbers). Standard SAP tables, fields, transaction codes allowed.
- Non-standard entities: Avoid mentioning non-standard entities (e.g. Z*, Y*, /SKN/*, /ABC4C/*) unless absolutely necessary. Prefer standard SAP object names.
- Parameter scope: Mention ONLY parameters that appear in the Parameters (Name (Description)) file for this EI. Do not refer to other parameters even if they appear in the code or Selected parameters file.
- No "output only" / "not a filter": Do not describe any parameter as "output only," "not a filter parameter," "used for output display only," or similar. All parameters can be used for filtering (function or frontend/post-processing). That distinction is irrelevant; do not state it.
- Currency parameters: For currency parameters (e.g. WAERS, WAERK, WAERK_FR), describe only business meaning: document/local currency, foreign currency, transaction currency, or other type. Do not include technical details (table names, field names, joins).
- Output: ONLY "### Parameter Configuration Guidelines" (heading + IMPORTANT line + all parameter blocks in order). No other sections.

Benchmark (structure only; infer content from inputs). Source: reference files\Explanation_Credit Memo Monthly volume by Payer_$1M in LC_200019_000012__EI__SW_10_01_ORD_VAL_TOT.md — subsection "### Parameter Configuration Guidelines".

Output structure / fields file location or paste:

Parameters (Name (Description)) file location or paste:

ABAP code file location or paste:

Selected parameters file: C:\vibe code dev\ei_auto_doc\reference\_Parameters.docx (if LLM cannot read .docx, paste relevant parameter explanations or provide text export)
---

Respond with ONLY the "### Parameter Configuration Guidelines" subsection (heading + IMPORTANT line + all parameter blocks in order). No other text.
